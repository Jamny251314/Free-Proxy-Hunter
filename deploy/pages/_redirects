# Cloudflare Pages · 让 /api 保持同源
# ---------------------------------------------------------------
# 用途：把前端静态产物托管到 Cloudflare Pages，后端仍留在自己的服务器上。
#
# 为什么推荐这个而不是「跨域直连后端」：
#   · 页面与 API 依然同源 → 浏览器不触发跨域，后端不需要配 CORS_ORIGINS
#   · 前端里的 /api/... 相对路径原样可用 → 构建时不需要 VITE_API_BASE_URL
#   · 少一处配置就少一处出错的地方（跨域 + SSE 的组合尤其容易踩坑）
#
# 用法：把这个文件放到前端产物根目录。
#   Vite 项目：放到 public/_redirects，`npm run build` 时会自动复制进 dist/
#   然后在 Cloudflare Pages 里把构建输出目录设为 dist
#
# 注意：规则是**从上往下**匹配的，/api/* 必须在 /* 之前，否则会被 SPA 兜底规则吃掉。
#
# SSE 提示：/api/chat 与 /api/proxy/stream 是长连接，经过 Pages 这层边缘转发时
# 依赖 20 秒一次的心跳保活（应用已内置）。若发现对话流会中途停住，
# 就改用下面的「方案 B」，把前端也放回后端一起托管。

/api/*  https://hunter-api.example.com/api/:splat  200
/*      /index.html                                 200

# ---------------------------------------------------------------------------
# 方案 B：不想让 Pages 转发 API，改用跨域直连
# ---------------------------------------------------------------------------
# 1) 构建前端时注入后端地址：
#      VITE_API_BASE_URL=https://hunter-api.example.com npm run build
# 2) 后端加上允许的前端来源（逗号分隔）：
#      CORS_ORIGINS=https://your-project.pages.dev
# 3) 删掉上面那行 /api/* 规则，只保留 SPA 兜底。
#
# 两种方案二选一即可。方案 A（本文件默认）少配置、无跨域，优先选它。
